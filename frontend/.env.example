# Vite only reads .env files from this directory (frontend/), not the repo
# root — copy this to frontend/.env for `npm run dev` outside Docker.
VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-anon-key
